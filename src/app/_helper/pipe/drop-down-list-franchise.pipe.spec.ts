import { DropDownListFranchisePipe } from './drop-down-list-franchise.pipe';

describe('DropDownListFranchisePipe', () => {
  it('create an instance', () => {
    const pipe = new DropDownListFranchisePipe();
    expect(pipe).toBeTruthy();
  });
});
